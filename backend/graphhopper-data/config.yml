graphhopper:
  datareader.file: /data/osm/ile-de-france-foot-transit.osm.pbf  # https://download.geofabrik.de/europe/france/ile-de-france-latest.osm.pbf
  graph.location: /data/graphs
  gtfs.file: /data/gtfs/IDFM-gtfs-filtered.zip   # https://eu.ftp.opendatasoft.com/stif/GTFS/IDFM-gtfs.zip
  import.osm.ignored_highways: motorway,trunk,primary,secondary
  profiles:
    - name: foot
      custom_model_files:
        - foot.json
    - name: pt
      custom_model: {"speed": [{ "if": "true", "limit_to": "foot_average_speed" }]}
  profiles_ch:
    - profile: foot
    - profile: pt
  graph.encoded_values: foot_access, foot_average_speed, foot_priority, foot_road_access, hike_rating, mtb_rating, country, road_class
  graph.dataaccess: MMAP_STORE
  
  prepare.min_network_size: 200
  prepare.min_one_way_network_size: 0
  
  # GTFS-specific settings optimized for frequency-based routing
  gtfs.max_transfer_distance: 400
  gtfs.walk_speed: 5.0
  gtfs.data_import: true
  
  # Frequency-based routing settings
  gtfs.max_wait_time: 600  # Maximum wait time (10 minutes)
  gtfs.min_transfer_time: 120  # Minimum transfer time (2 minutes)
  gtfs.use_frequencies: true  # Enable frequency-based routing
  gtfs.ignore_real_time: true  # Ignore schedule variations
  
  # Optimize for frequency-based queries
  graph.do_sort: true
  graph.optimize_storage: true
  graph.import_threads: 4
  graph.precalculate: true
  graph.precalculate_distance: 500
  
  datareader:
    gtfs:
      base-osm-filter: true
      walk-speed: 5.0
      use-optimized-transfers: true  
      transfers-min-transfer-time: 60
      
      # Frequency-based optimizations
      use-frequencies: true
      ignore-stop-times: false  # Keep minimal stop_times for connectivity
      default-headway: 600  # Default 10-minute headway
      
      # Clustering and simplification
      cluster-stops: true  
      cluster-stops-max-distance: 100
      cluster-stops-max-transfer-time: 90
      fare-approximation: false
      transfer-time-cutoff: 1200
      memory-mapped: true
      skip-stops-outside-border: true
      skip-shapes: false  # Keep shapes for route visualization
      skip-trips-without-shapes: false

server:
  application_connectors:
    - type: http
      port: 8989
      bind_host: 0.0.0.0
  request_log:
    appenders: []
  max_threads: 32