export { MapService } from './interfaces';
// Export types for TypeScript users (types don't actually export anything in JavaScript)
// but this signals intent and documents the structure
export * from './types';